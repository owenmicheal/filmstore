import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    image: {
        maxWidth: '80%',
        objectFit: 'cover',
        borderRadius: '25px',
        boxShadow: '0.5em 0.5em 1em',
    }
}));